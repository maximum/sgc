<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) || "SGC" %></title>
  <%= stylesheet_link_tag "application", "scaffold-modified" %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
    <%= yield(:head) %>
</head>
<body>	
	<div id="container">	
  <div id="user_nav">
    <% if current_user %>
          Bem-vindo <%=h current_user.login %>!
      <%= link_to "Editar usuário", edit_user_path(:current) %> |
      <%= link_to "Logout", logout_path %>
    <% else %>
      <%= link_to "Registrar Candidato", new_user_path(:type => 'candidato') %> |
      <%= link_to "Login", login_path %>
    <% end %>
  </div>
  <%- flash.each do |name, msg| -%>
    <%= content_tag :div, msg, :id => "flash_#{name}" %>
  <%- end -%>
  
  <%- if show_title? -%>
    <h1><%=h yield(:title) %></h1>
  <%- end -%>
  
		<h1><a href="/">SGC - Sistema de Gestão de Carreira</a></h1>	
	
		<%= yield %>
		</div>
</body>
</html>
