<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) || "SGC" %></title>
  <%= stylesheet_link_tag "application", "scaffold-modified", "home" %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
    <%= yield(:head) %>
</head>
<body>	
	<div id="container">
  <div id="user_nav">
    <% if current_user %>
          Bem-vindo <%=h current_user.userble.nome %>!
      <%#= current_user.userble.class == "Candidato" ? (link_to "Editar usuário", edit_candidato_path) : (link_to "Editar usuário", edit_empresa_path) %> |
      <%= link_to "Logout", logout_path %>
    <% else %>
      <%= link_to "Registrar Candidato", new_candidato_path %> |      
      <%= link_to "Registrar Empresa", new_empresa_path %> |
      <%= link_to "Login", login_path %>
    <% end %>
  </div>
  <%- flash.each do |name, msg| -%>
    <%= content_tag :div, msg, :id => "flash_#{name}" %>
  <%- end -%>
		<h1><%= if current_user then link_to "SGC - Sistema de Gestão de Carreira", current_user.userble end %></h1>
  <%- if show_title? -%>
    <h1><%=h yield(:title) %></h1>
  <%- end -%>
  	
		<%= yield %>
		</div>
</body>
</html>
